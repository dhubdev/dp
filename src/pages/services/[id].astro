---

import { getEntry } from "astro:content";

const { id } = Astro.params

const entry = await getEntry("services", id as any)

if (entry === undefined) {
  return Astro.redirect("/404")
}

const { Content } = await entry.render()
---

<Content />